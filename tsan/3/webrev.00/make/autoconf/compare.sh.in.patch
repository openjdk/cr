diff a/make/autoconf/compare.sh.in b/make/autoconf/compare.sh.in
--- a/make/autoconf/compare.sh.in
+++ b/make/autoconf/compare.sh.in
@@ -65,11 +65,10 @@
 export STAT="@STAT@"
 export STRIP="@STRIP@ @STRIPFLAGS@"
 export TAR="@TAR@"
 export TEE="@TEE@"
 export UNIQ="@UNIQ@"
-export UNPACK200="@FIXPATH@ @BOOT_JDK@/bin/unpack200"
 export UNARCHIVE="@UNZIP@ -q -o"
 
 export TOPDIR="@TOPDIR@"
 export OUTPUTDIR="@OUTPUTDIR@"
 
