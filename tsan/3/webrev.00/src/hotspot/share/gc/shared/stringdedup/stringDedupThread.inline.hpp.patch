diff a/src/hotspot/share/gc/shared/stringdedup/stringDedupThread.inline.hpp b/src/hotspot/share/gc/shared/stringdedup/stringDedupThread.inline.hpp
--- a/src/hotspot/share/gc/shared/stringdedup/stringDedupThread.inline.hpp
+++ b/src/hotspot/share/gc/shared/stringdedup/stringDedupThread.inline.hpp
@@ -31,11 +31,15 @@
 
 template <typename S>
 void StringDedupThreadImpl<S>::do_deduplication() {
   S total_stat;
 
-  deduplicate_shared_strings(&total_stat);
+  {
+    // Block safepoints while deduplicating shared strings
+    SuspendibleThreadSetJoiner sts_join;
+    deduplicate_shared_strings(&total_stat);
+  }
 
   // Main loop
   for (;;) {
     S stat;
 
