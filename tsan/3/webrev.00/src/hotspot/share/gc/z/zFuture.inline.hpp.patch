diff a/src/hotspot/share/gc/z/zFuture.inline.hpp b/src/hotspot/share/gc/z/zFuture.inline.hpp
--- a/src/hotspot/share/gc/z/zFuture.inline.hpp
+++ b/src/hotspot/share/gc/z/zFuture.inline.hpp
@@ -26,19 +26,28 @@
 
 #include "gc/z/zFuture.hpp"
 #include "runtime/semaphore.inline.hpp"
 #include "runtime/thread.hpp"
 
+template <typename T>
+inline ZFuture<T>::ZFuture() :
+    _value() {}
+
 template <typename T>
 inline void ZFuture<T>::set(T value) {
   // Set value
   _value = value;
 
   // Notify waiter
   _sema.signal();
 }
 
+template <typename T>
+inline T ZFuture<T>::peek() {
+  return _value;
+}
+
 template <typename T>
 inline T ZFuture<T>::get() {
   // Wait for notification
   Thread* const thread = Thread::current();
   if (thread->is_Java_thread()) {
