diff a/src/java.security.jgss/windows/classes/sun/security/krb5/internal/tools/KinitOptions.java b/src/java.security.jgss/windows/classes/sun/security/krb5/internal/tools/KinitOptions.java
--- a/src/java.security.jgss/windows/classes/sun/security/krb5/internal/tools/KinitOptions.java
+++ b/src/java.security.jgss/windows/classes/sun/security/krb5/internal/tools/KinitOptions.java
@@ -191,12 +191,12 @@
                 }
             }
             PrincipalName p = cis.readPrincipal(version);
             cis.close();
             if (DEBUG) {
-                System.out.println(">>>KinitOptions principal name from "+
-                                   "the cache is :" + p);
+                System.out.println(">>>KinitOptions principal name from " +
+                                   "the cache is: " + p);
             }
             return p;
         } catch (IOException e) {
             // ignore any exceptions; we will use the user name as the
             // principal name
@@ -209,11 +209,11 @@
             }
         }
 
         String username = System.getProperty("user.name");
         if (DEBUG) {
-            System.out.println(">>>KinitOptions default username is :"
+            System.out.println(">>>KinitOptions default username is: "
                                + username);
         }
         try {
             PrincipalName p = new PrincipalName(username);
             return p;
