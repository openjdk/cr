diff a/src/java.security.jgss/share/classes/sun/security/krb5/internal/crypto/EType.java b/src/java.security.jgss/share/classes/sun/security/krb5/internal/crypto/EType.java
--- a/src/java.security.jgss/share/classes/sun/security/krb5/internal/crypto/EType.java
+++ b/src/java.security.jgss/share/classes/sun/security/krb5/internal/crypto/EType.java
@@ -61,11 +61,11 @@
             allowed = cfg.getBooleanObject("libdefaults", "allow_weak_crypto")
                     == Boolean.TRUE;
         } catch (Exception exc) {
             if (DEBUG) {
                 System.out.println ("Exception in getting allow_weak_crypto, " +
-                                    "using default value " +
+                                    "using default value: " +
                                     exc.getMessage());
             }
         }
         allowWeakCrypto = allowed;
     }
@@ -254,11 +254,11 @@
         try {
             config = Config.getInstance();
         } catch (KrbException exc) {
             if (DEBUG) {
                 System.out.println("Exception while getting " +
-                    configName + exc.getMessage());
+                    configName + ": " + exc.getMessage());
                 System.out.println("Using default builtin etypes");
             }
             return getBuiltInDefaults();
         }
         return config.defaultEtype(configName);
