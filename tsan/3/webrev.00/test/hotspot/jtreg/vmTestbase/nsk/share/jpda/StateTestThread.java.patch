diff a/test/hotspot/jtreg/vmTestbase/nsk/share/jpda/StateTestThread.java b/test/hotspot/jtreg/vmTestbase/nsk/share/jpda/StateTestThread.java
--- a/test/hotspot/jtreg/vmTestbase/nsk/share/jpda/StateTestThread.java
+++ b/test/hotspot/jtreg/vmTestbase/nsk/share/jpda/StateTestThread.java
@@ -109,29 +109,29 @@
         case 1:
             // start thread
             start();
 
             while (!isRunning)
-                yield();
+                Thread.yield();
 
             break;
         case 2:
             // stop running
             isRunning = false;
 
             while (this.getState() != Thread.State.TIMED_WAITING)
-                yield();
+                Thread.yield();
 
             break;
         case 3:
             waitState = true;
 
             // stop sleeping
             interrupt();
 
             while (getState() != Thread.State.WAITING)
-                yield();
+                Thread.yield();
 
             break;
         case 4:
             waitState = false;
 
@@ -139,11 +139,11 @@
             synchronized (waitOnObject) {
                 waitOnObject.notify();
             }
 
             while (!readyToBeBlocked || (getState() != Thread.State.BLOCKED))
-                yield();
+                Thread.yield();
 
             break;
         case 5:
             // let StateTestThread thread acquire lock
             auxiliaryThread.releaseLock();
