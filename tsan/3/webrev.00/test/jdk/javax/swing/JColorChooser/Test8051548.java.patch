diff a/test/jdk/javax/swing/JColorChooser/Test8051548.java b/test/jdk/javax/swing/JColorChooser/Test8051548.java
--- a/test/jdk/javax/swing/JColorChooser/Test8051548.java
+++ b/test/jdk/javax/swing/JColorChooser/Test8051548.java
@@ -54,15 +54,20 @@
     };
 
     private static JColorChooser colorChooser;
     private static boolean propertyChangeListenerInvoked;
     private static volatile Color color;
+    private static JFrame frame;
 
     public static void main(String[] args) throws Exception {
-        testColorPanels();
-        testShowDialog(true);
-        testShowDialog(false);
+        try {
+            testColorPanels();
+            testShowDialog(true);
+            testShowDialog(false);
+        } finally {
+            if(frame != null) SwingUtilities.invokeAndWait(() -> frame.dispose());
+        }
     }
 
     private static void testColorPanels() throws Exception {
         SwingUtilities.invokeAndWait(() -> createAndShowGUI());
 
@@ -180,11 +185,11 @@
     private static boolean colorHasAlpha(){
         return colorChooser.getColor().getAlpha() != 255;
     }
 
     private static void createAndShowGUI() {
-        JFrame frame = new JFrame();
+        frame = new JFrame();
         frame.setSize(700, 500);
         frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
         colorChooser = new JColorChooser();
         frame.getContentPane().add(colorChooser);
         frame.setVisible(true);
