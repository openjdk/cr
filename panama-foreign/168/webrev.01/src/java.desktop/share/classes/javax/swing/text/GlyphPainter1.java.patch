diff a/src/java.desktop/share/classes/javax/swing/text/GlyphPainter1.java b/src/java.desktop/share/classes/javax/swing/text/GlyphPainter1.java
--- a/src/java.desktop/share/classes/javax/swing/text/GlyphPainter1.java
+++ b/src/java.desktop/share/classes/javax/swing/text/GlyphPainter1.java
@@ -174,12 +174,11 @@
         int p1 = v.getEndOffset();
         TabExpander expander = v.getTabExpander();
         Segment text = v.getText(p0, p1);
         int[] justificationData = getJustificationData(v);
         int offs = Utilities.getTabbedTextOffset(v, text, metrics,
-                                                 alloc.x, (int) x, expander, p0,
-                                                 justificationData);
+                (float)alloc.x,  x, expander, p0, justificationData);
         SegmentCache.releaseSharedSegment(text);
         int retValue = p0 + offs;
         if(retValue == p1) {
             // No need to return backward bias as GlyphPainter1 is used for
             // ltr text only.
