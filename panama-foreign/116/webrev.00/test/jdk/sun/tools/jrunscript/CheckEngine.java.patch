diff a/test/jdk/sun/tools/jrunscript/CheckEngine.java b/test/jdk/sun/tools/jrunscript/CheckEngine.java
--- a/test/jdk/sun/tools/jrunscript/CheckEngine.java
+++ b/test/jdk/sun/tools/jrunscript/CheckEngine.java
@@ -29,17 +29,8 @@
  * the jrunscript tests which assume a js engine can be vacuously
  * passed.
  */
 public class CheckEngine {
     public static void main(String... args) {
-        int exitCode = 0;
-        ScriptEngine engine =
-            (new ScriptEngineManager()).getEngineByName("nashorn");
-
-        if (engine == null &&
-            !(System.getProperty("java.runtime.name").startsWith("Java(TM)"))) {
-            exitCode = 2;
-        }
-
-        System.exit(exitCode);
+        System.exit(2);
     }
 }
