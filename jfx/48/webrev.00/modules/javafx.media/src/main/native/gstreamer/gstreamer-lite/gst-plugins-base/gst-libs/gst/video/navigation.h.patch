diff a/modules/javafx.media/src/main/native/gstreamer/gstreamer-lite/gst-plugins-base/gst-libs/gst/video/navigation.h b/modules/javafx.media/src/main/native/gstreamer/gstreamer-lite/gst-plugins-base/gst-libs/gst/video/navigation.h
--- a/modules/javafx.media/src/main/native/gstreamer/gstreamer-lite/gst-plugins-base/gst-libs/gst/video/navigation.h
+++ b/modules/javafx.media/src/main/native/gstreamer/gstreamer-lite/gst-plugins-base/gst-libs/gst/video/navigation.h
@@ -194,11 +194,11 @@
  * changes and should re-queried by interested applications.
  * @GST_NAVIGATION_MESSAGE_ANGLES_CHANGED: Sent when display angles in a multi-angle
  * feature (such as a multiangle DVD) change - either angles have appeared or
  * disappeared.
  * @GST_NAVIGATION_MESSAGE_EVENT: Sent when a navigation event was not handled
- * by any element in the pipeline (Since 1.6)
+ * by any element in the pipeline (Since: 1.6)
  *
  * A set of notifications that may be received on the bus when navigation
  * related status changes.
  */
 typedef enum {
@@ -233,15 +233,15 @@
                                                              guint *cur_angle,
                                                              guint *n_angles);
 
 GST_VIDEO_API
 GstMessage *    gst_navigation_message_new_event            (GstObject *src,
-                                 GstEvent *event);
+                   GstEvent *event);
 
 GST_VIDEO_API
 gboolean        gst_navigation_message_parse_event          (GstMessage *message,
-                                 GstEvent ** event);
+                   GstEvent ** event);
 /* event parsing functions */
 /**
  * GstNavigationEventType:
  * @GST_NAVIGATION_EVENT_INVALID: Returned from
  * gst_navigation_event_get_type() when the passed event is not a navigation event.
