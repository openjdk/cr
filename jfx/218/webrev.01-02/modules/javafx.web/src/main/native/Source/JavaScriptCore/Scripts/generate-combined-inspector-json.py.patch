diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/Scripts/generate-combined-inspector-json.py b/modules/javafx.web/src/main/native/Source/JavaScriptCore/Scripts/generate-combined-inspector-json.py
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/Scripts/generate-combined-inspector-json.py
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/Scripts/generate-combined-inspector-json.py
@@ -24,10 +24,11 @@
 # THE POSSIBILITY OF SUCH DAMAGE.
 
 import glob
 import json
 import os
+import re
 import sys
 
 if len(sys.argv) < 2:
     print("usage: %s [json files or directory of json files ...]" % os.path.basename(sys.argv[0]))
     sys.exit(1)
@@ -55,11 +56,12 @@
         print(",")
 
     string = open(file).read()
 
     try:
-        dictionary = json.loads(string)
+        regex = re.compile(r"\/\*.*?\*\/", re.DOTALL)
+        dictionary = json.loads(re.sub(regex, "", string))
         if not "domain" in dictionary:
             raise Exception("File \"%s\" does not contains a \"domain\" key." % file)
     except ValueError:
         sys.stderr.write("File \"%s\" does not contain valid JSON:\n" % file)
         raise
