diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/heap/HeapCell.cpp b/modules/javafx.web/src/main/native/Source/JavaScriptCore/heap/HeapCell.cpp
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/heap/HeapCell.cpp
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/heap/HeapCell.cpp
@@ -32,20 +32,20 @@
 
 namespace JSC {
 
 bool HeapCell::isLive()
 {
-    if (isLargeAllocation())
-        return largeAllocation().isLive();
+    if (isPreciseAllocation())
+        return preciseAllocation().isLive();
     auto& markedBlockHandle = markedBlock().handle();
     if (markedBlockHandle.isFreeListed())
         return !markedBlockHandle.isFreeListedCell(this);
     return markedBlockHandle.isLive(this);
 }
 
 #if !COMPILER(GCC_COMPATIBLE)
-void HeapCell::use() const
+NEVER_INLINE void keepAlive(const void*)
 {
 }
 #endif
 
 } // namespace JSC
