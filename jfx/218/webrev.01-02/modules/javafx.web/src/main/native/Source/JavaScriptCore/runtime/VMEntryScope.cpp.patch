diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/runtime/VMEntryScope.cpp b/modules/javafx.web/src/main/native/Source/JavaScriptCore/runtime/VMEntryScope.cpp
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/runtime/VMEntryScope.cpp
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/runtime/VMEntryScope.cpp
@@ -40,11 +40,10 @@
 VMEntryScope::VMEntryScope(VM& vm, JSGlobalObject* globalObject)
     : m_vm(vm)
     , m_globalObject(globalObject)
 {
     ASSERT(!DisallowVMReentry::isInEffectOnCurrentThread());
-    ASSERT(Thread::current().stack().isGrowingDownward());
     if (!vm.entryScope) {
         vm.entryScope = this;
 
         // Reset the date cache between JS invocations to force the VM to
         // observe time zone changes.
