diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/llint/LowLevelInterpreter.h b/modules/javafx.web/src/main/native/Source/JavaScriptCore/llint/LowLevelInterpreter.h
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/llint/LowLevelInterpreter.h
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/llint/LowLevelInterpreter.h
@@ -30,11 +30,11 @@
 #if ENABLE(C_LOOP)
 
 namespace JSC {
 
 // The following is a set of alias for the opcode names. This is needed
-// because there is code (e.g. in GetByIdStatus.cpp and PutByIdStatus.cpp)
+// because there is code (e.g. in GetByStatus.cpp and PutByIdStatus.cpp)
 // which refers to the opcodes expecting them to be prefixed with "llint_".
 // In the CLoop implementation, the 2 are equivalent. Hence, we set up this
 // alias here.
 
 #define LLINT_OPCODE_ALIAS(opcode, length) \
