diff a/modules/javafx.web/src/main/native/Source/WebCore/rendering/style/ShapeValue.cpp b/modules/javafx.web/src/main/native/Source/WebCore/rendering/style/ShapeValue.cpp
--- a/modules/javafx.web/src/main/native/Source/WebCore/rendering/style/ShapeValue.cpp
+++ b/modules/javafx.web/src/main/native/Source/WebCore/rendering/style/ShapeValue.cpp
@@ -33,11 +33,11 @@
 
 bool ShapeValue::isImageValid() const
 {
     if (!m_image)
         return false;
-    if (m_image->isCachedImage()) {
+    if (m_image->hasCachedImage()) {
         auto* cachedImage = m_image->cachedImage();
         return cachedImage && cachedImage->hasImage();
     }
     return m_image->isGeneratedImage();
 }
