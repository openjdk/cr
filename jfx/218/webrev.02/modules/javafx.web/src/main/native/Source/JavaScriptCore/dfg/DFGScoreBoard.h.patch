diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/dfg/DFGScoreBoard.h b/modules/javafx.web/src/main/native/Source/JavaScriptCore/dfg/DFGScoreBoard.h
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/dfg/DFGScoreBoard.h
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/dfg/DFGScoreBoard.h
@@ -58,11 +58,11 @@
         std::sort(m_free.begin(), m_free.end());
     }
 
     void assertClear()
     {
-        if (ASSERT_DISABLED)
+        if (!ASSERT_ENABLED)
             return;
 
         // For every entry in the used list the use count of the virtual register should be zero, or max, due to it being a preserved local.
         for (size_t i = 0; i < m_used.size(); ++i)
             RELEASE_ASSERT(!m_used[i] || m_used[i] == max());
