diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/assembler/ARM64Registers.h b/modules/javafx.web/src/main/native/Source/JavaScriptCore/assembler/ARM64Registers.h
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/assembler/ARM64Registers.h
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/assembler/ARM64Registers.h
@@ -37,11 +37,11 @@
 // We don't include LR in the set of callee-save registers even though it technically belongs
 // there. This is because we use this set to describe the set of registers that need to be saved
 // beyond what you would save by the platform-agnostic "preserve return address" and "restore
 // return address" operations in CCallHelpers.
 
-#if !PLATFORM(IOS_FAMILY)
+#if !OS(DARWIN)
 #define FOR_EACH_GP_REGISTER(macro)                             \
     /* Parameter/result registers. */                           \
     macro(x0,  "x0",  0, 0)                                     \
     macro(x1,  "x1",  0, 0)                                     \
     macro(x2,  "x2",  0, 0)                                     \
