diff a/modules/javafx.web/src/main/native/Source/WebCore/page/RenderingUpdateScheduler.h b/modules/javafx.web/src/main/native/Source/WebCore/page/RenderingUpdateScheduler.h
--- a/modules/javafx.web/src/main/native/Source/WebCore/page/RenderingUpdateScheduler.h
+++ b/modules/javafx.web/src/main/native/Source/WebCore/page/RenderingUpdateScheduler.h
@@ -48,14 +48,17 @@
     RenderingUpdateScheduler(Page&);
     void scheduleTimedRenderingUpdate();
     void scheduleImmediateRenderingUpdate();
     void scheduleRenderingUpdate();
 
+#if USE(REQUEST_ANIMATION_FRAME_DISPLAY_MONITOR)
+    void windowScreenDidChange(PlatformDisplayID);
+#endif
+
 private:
 #if USE(REQUEST_ANIMATION_FRAME_DISPLAY_MONITOR)
     RefPtr<DisplayRefreshMonitor> createDisplayRefreshMonitor(PlatformDisplayID) const final;
-    void windowScreenDidChange(PlatformDisplayID);
     void displayRefreshFired() final;
 #else
     void displayRefreshFired();
 #endif
 
