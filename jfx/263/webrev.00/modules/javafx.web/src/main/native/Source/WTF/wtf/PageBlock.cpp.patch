diff a/modules/javafx.web/src/main/native/Source/WTF/wtf/PageBlock.cpp b/modules/javafx.web/src/main/native/Source/WTF/wtf/PageBlock.cpp
--- a/modules/javafx.web/src/main/native/Source/WTF/wtf/PageBlock.cpp
+++ b/modules/javafx.web/src/main/native/Source/WTF/wtf/PageBlock.cpp
@@ -42,11 +42,11 @@
 
 #if OS(UNIX)
 
 inline size_t systemPageSize()
 {
-    return getpagesize();
+    return sysconf(_SC_PAGESIZE);
 }
 
 #elif OS(WINDOWS)
 
 inline size_t systemPageSize()
@@ -60,13 +60,15 @@
 
 #endif
 
 size_t pageSize()
 {
-    if (!s_pageSize)
+    if (!s_pageSize) {
         s_pageSize = systemPageSize();
-    ASSERT(isPowerOfTwo(s_pageSize));
+        RELEASE_ASSERT(isPowerOfTwo(s_pageSize));
+        RELEASE_ASSERT_WITH_MESSAGE(s_pageSize <= CeilingOnPageSize, "CeilingOnPageSize is too low, raise it in PageBlock.h!");
+    }
     return s_pageSize;
 }
 
 size_t pageMask()
 {
