diff a/modules/javafx.web/src/main/native/Source/bmalloc/bmalloc/DeferredTrigger.h b/modules/javafx.web/src/main/native/Source/bmalloc/bmalloc/DeferredTrigger.h
--- a/modules/javafx.web/src/main/native/Source/bmalloc/bmalloc/DeferredTrigger.h
+++ b/modules/javafx.web/src/main/native/Source/bmalloc/bmalloc/DeferredTrigger.h
@@ -24,10 +24,12 @@
  */
 
 #pragma once
 
 #include "IsoPageTrigger.h"
+#include "Mutex.h"
+#include <mutex>
 
 namespace bmalloc {
 
 template<typename Config> class IsoPage;
 
@@ -35,14 +37,14 @@
 class DeferredTrigger {
 public:
     DeferredTrigger() { }
 
     template<typename Config>
-    void didBecome(IsoPage<Config>&);
+    void didBecome(const LockHolder&, IsoPage<Config>&);
 
     template<typename Config>
-    void handleDeferral(IsoPage<Config>&);
+    void handleDeferral(const LockHolder&, IsoPage<Config>&);
 
 private:
     bool m_hasBeenDeferred { false };
 };
 
