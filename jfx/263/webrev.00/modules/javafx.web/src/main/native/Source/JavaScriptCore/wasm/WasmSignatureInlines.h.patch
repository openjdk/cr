diff a/modules/javafx.web/src/main/native/Source/JavaScriptCore/wasm/WasmSignatureInlines.h b/modules/javafx.web/src/main/native/Source/JavaScriptCore/wasm/WasmSignatureInlines.h
--- a/modules/javafx.web/src/main/native/Source/JavaScriptCore/wasm/WasmSignatureInlines.h
+++ b/modules/javafx.web/src/main/native/Source/JavaScriptCore/wasm/WasmSignatureInlines.h
@@ -46,11 +46,11 @@
     return *bitwise_cast<const Signature*>(index);
 }
 
 inline SignatureIndex SignatureInformation::get(const Signature& signature)
 {
-    if (!ASSERT_DISABLED) {
+    if (ASSERT_ENABLED) {
         SignatureInformation& info = singleton();
         auto locker = holdLock(info.m_lock);
         ASSERT_UNUSED(info, info.m_signatureSet.contains(SignatureHash { makeRef(const_cast<Signature&>(signature)) }));
     }
     return bitwise_cast<SignatureIndex>(&signature);
