diff a/modules/javafx.web/src/main/native/Source/ThirdParty/icu/source/common/uvector.cpp b/modules/javafx.web/src/main/native/Source/ThirdParty/icu/source/common/uvector.cpp
--- a/modules/javafx.web/src/main/native/Source/ThirdParty/icu/source/common/uvector.cpp
+++ b/modules/javafx.web/src/main/native/Source/ThirdParty/icu/source/common/uvector.cpp
@@ -332,19 +332,19 @@
     if (minimumCapacity < 0) {
         status = U_ILLEGAL_ARGUMENT_ERROR;
         return FALSE;
     }
     if (capacity < minimumCapacity) {
-        if (capacity > (INT32_MAX - 1) / 2) {           // integer overflow check
+        if (capacity > (INT32_MAX - 1) / 2) {            // integer overflow check
             status = U_ILLEGAL_ARGUMENT_ERROR;
             return FALSE;
         }
         int32_t newCap = capacity * 2;
         if (newCap < minimumCapacity) {
             newCap = minimumCapacity;
         }
-        if (newCap > (int32_t)(INT32_MAX / sizeof(UElement))) { // integer overflow check
+        if (newCap > (int32_t)(INT32_MAX / sizeof(UElement))) {    // integer overflow check
             // We keep the original memory contents on bad minimumCapacity.
             status = U_ILLEGAL_ARGUMENT_ERROR;
             return FALSE;
         }
         UElement* newElems = (UElement *)uprv_realloc(elements, sizeof(UElement)*newCap);
